<!DOCTYPE html>
<html lang="en">
<head>
  <%- include('partials/head') %>
</head>
<body id="catalog">
    <%- include('partials/header') %>
    <h2>
        Delete <%= item.name %>
    </h2>
    <form method="POST" action="" id="delete-form">
        <div>
            <label for="password">
                <strong>
                Admin password:
                </strong>
            </label>
            <input 
                id="password"
                name="password"
                type="password"
                required
            />
        </div>
        <h2>
            <strong>
                Please confirm that you would like to delete this item.
            </strong>
        </h2>
        <button type="submit">
            Confirm
        </button>
    </form>
    <% if (error) { %>
        <ul>
            <li>
                <%= error.msg %>
            </li>
        </ul>
    <% } %>
</body>
</html>